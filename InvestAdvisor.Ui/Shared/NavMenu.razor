<MudNavMenu>
    @foreach (var d in NavDestinations.All)
    {
        <MudNavLink Href="@d.Href" Match="@(d.Href == "/" ? NavLinkMatch.All : NavLinkMatch.Prefix)" Icon="@d.Icon">@d.Label</MudNavLink>
    }
</MudNavMenu>
